import React from 'react'
import MovieContainer from './components/MovieContainer'

const App = (props) => {
    return (
        <div>
            <MovieContainer />
        </div>
    )
}

export default App
